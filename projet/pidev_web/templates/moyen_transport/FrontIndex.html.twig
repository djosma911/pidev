{% extends 'Front/front.html.twig'%}

{% block title %}Moyen de Transport{% endblock %}

{% block body %}
<div id="main">
           <!-- Post -->
	 <section class="post">
		  <header class="major">
<div class="row" style="display: flex;align-items: center;justify-content: center;margin: 100px 300px 100px">
    <div class="col-lg-15">
    <div class="card">
    <div class="card-body">
{# <div  style="text-align: center;"> #}
    <h1>Les lignes urbaines</h1>
      <a href="{{ path('app_moyen_transport_new') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px"></i></a>
               <a class="btn btn-success" href="{{path('app_bot') }}">  BOT </a>

    <div class="table-responsive">
    <table class="table table-striped table-light table-bordered" id="t">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom ligne</th>
                <th>Type igne</th>
                <th>actions</th>
                <th>Supprimer</th>
            </tr>
        </thead>
        <tbody id="myTable">
        {% for ligne in lignes %}
            <tr>
                <td>{{ ligne.id }}</td>
                <td>{{ ligne.nomLigne }}</td>
                <td>{{ ligne.typeLigne }}</td>
                <td>
                    <a class="btn btn-outline-success" href="{{ path('app_ligne_show', {'id': ligne.id}) }}">show</a>
                    <a class="btn btn-outline-info" href="{{ path('app_ligne_edit', {'id': ligne.id}) }}">edit</a>
                    
                </td>
                <td>
                {{ include('ligne/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        <input type="text" value="" class="form-control" placeholder="Search for..." id="myInput">
    <button type="submit" class="btn btn-default btn-round btn-just-icon">
        </tbody>
    </table>
    {# </div> #}
    </div>
    </div>
    </div>
    </div>
    </div>
    		  </header>
       
    </section>
{% endblock %}
